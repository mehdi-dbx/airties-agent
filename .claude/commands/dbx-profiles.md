List all configured Databricks CLI profiles with their host and validity status.

Run this bash command and display the output in a clean table:

```bash
databricks auth profiles
```

Present the results clearly. Highlight which profiles are valid (YES) vs invalid (NO). If any profiles are valid, mention them prominently so the user knows which ones are ready to use.
